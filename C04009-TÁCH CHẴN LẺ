#include <stdio.h>
int main(){
	int n;
	scanf("%d",&n);
	int a[n],b[n],c[n],e=0,f=0;
	for(int i=0;i<n;i++){
		scanf("%d",&a[i]);
		if(a[i]%2==0) b[e++]=a[i];
		else c[f++]=a[i];
	}
	for(int i=0;i<e;i++) printf("%d ",b[i]);
	printf("\n");
	for(int i=0;i<f;i++) printf("%d ",c[i]);
}
